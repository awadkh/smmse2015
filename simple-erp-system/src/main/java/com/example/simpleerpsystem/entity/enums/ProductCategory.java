package com.example.simpleerpsystem.entity.enums;

public enum ProductCategory {
    WATER_FILTER,
    FILTER_PART,
    AC_UNIT,
    AC_PART,
    SERVICE // For sellable services that might not have stock
}
